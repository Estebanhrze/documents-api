[project]
name = "documents-api"
version = "0.1.0"
description = "documents"
requires-python = ">=3.10"

dependencies = [
   "fastapi[standard]",
   "pydantic==2.13.4",
   "python-dotenv==1.2.2",
   "supabase==2.31.0"
 ]

[tool.fastapi]
entrypoint = "app.main:app"
